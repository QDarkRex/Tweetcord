discord.py==2.7.1
# Yuuzi261's tweety fork patches upstream issue #295 (X's web-client rewrite broke
# x-client-transaction-id computation) with a REAL fix, so no dummy-id monkeypatch
# and no headless-browser sourcing is needed — the replies endpoint
# (UserTweetsAndReplies) works with the properly-computed transaction id.
tweety-ns @ git+https://github.com/Yuuzi261/tweety.git@aeee95370cb59745311743708dac486ad4643323
python-dotenv==1.2.2
pyyaml==6.0.1
aiosqlite==0.18.0
socksio==1.0.0  # enables socks5:// proxies for optional per-burner proxy support (proxy_patch.py)
